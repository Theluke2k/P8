clear
close all
clc

import casadi.*

% Create an opti stack
opti = Opti();

% Create decision variables
x = opti.variable();
y = 1;

z = y;

% Define objective function
opti.minimize(  );

% Because casadis default initial guess is 0,0.
opti.set_initial(x, [1;1]);    % so x(1)+x(2)=2, safe

% Configure printing for ipopt solver
opts = struct();
opts.ipopt.print_level = false;

% Choose the ipopt solver
opti.solver('ipopt', opts);
sol = opti.solve();
sol.value(x)
